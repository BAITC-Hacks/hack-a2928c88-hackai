# Использованные материалы

- Координационный harness: AGENTS.md, prompts/lead.md, scripts/agents.py, coord/agents.json, context/ и шаблоны взяты из заранее подготовленного C:/Users/Denry/Desktop/tracks/tracks/harness и templates. Это предсуществующий материал команды. Приложение старого harness не импортировалось.
- docs/CASE.md — предоставленный капитаном текст официального кейса AI Sana (исходная копия подготовлена Claude). Источник указан в файле.
- Pydantic 2.13.5 — валидация схем; pytest 9.1.1 — тесты. Остальные зависимости этих пакетов устанавливает pip.
- Синтетические задачи, команды, ответы и отклики: подготовка Claude, проверка/нормализация Codex. Не реальные компании, пользователи, данные или действующие прототипы; example.com — демонстрационные ссылки.
- AI используется для разработки: Codex — ведущий, Claude — совет/демо/данные. Gemini пытались запустить через Antigravity и Gemini CLI; оба недоступны, фактического ответа нет.
- Продуктовый AI API и деплой пока не реализованы; не заявляются работающими.

## Обновление реализации 23.09.2026

FastAPI 0.141.1, Uvicorn 0.53.0, OpenAI SDK 2.54.0, python-dotenv 1.2.3; httpx 0.28.1 для тестов. SQLite из стандартной библиотеки Python. Продуктовый AI реализован в коде: OpenAI gpt-4.1-mini (настраивается), NVIDIA meta/llama-3.3-70b-instruct (настраивается), mock. Реальные вызовы не проверены без ключей. Публичного деплоя пока нет. Предыдущее указание «не реализован» относится к состоянию до этого обновления.

- Playwright 1.63.0 — дополнительный инструмент локальной браузерной проверки, использован установленный Microsoft Edge. Не runtime-зависимость приложения. scripts/smoke_catalog.py создаёт синтетический отклик только на localhost.

- Полный PDF кейса передан капитаном 23.09.2026; сохранён без изменений. pypdf 6.19.0 использован локально только для извлечения ТЗ, не является runtime-зависимостью приложения. Docker Hub — приватный реестр артефакта деплоя, Git-исходники остаются в выданном репозитории.

- 23.09.2026: OpenAI gpt-4.1-mini использован в продукте для реальных запросов на синтетических данных. Проверены вопросы, извлечение цитат и полный локальный UI-сценарий. NVIDIA остаётся непроверенным резервом. .env и ключ не включены в коммит или Docker-образ.

- 23.09.2026: набор Claude расширен Codex на 400 шаблонных учебных задач и откликов через scripts/generate_synthetic_bulk.py. Всего 413 карточек, 10 команд, 417 откликов. Все данные вымышлены; CSV в описаниях не приложены, целевые метрики не являются результатами испытаний.

- 23.09.2026: редизайн интерфейса (app/static: ui.css, index.html, business.*, catalog.*) подготовлен с помощью Claude Code по плану команды; палитру задала команда. Шрифты Unbounded, Onest и JetBrains Mono (лицензия SIL OFL) подключаются с Google Fonts; без сети интерфейс работает на системных шрифтах. Рекомендации задач командам (app/recommend.py) — детерминированное совпадение слов профиля команды с текстом задачи, без AI.

- Стресс-тест карточки: OpenAI gpt-4.1-mini, Structured Outputs; отправляются поля карточки, store=False. Резерв — локальные явно помеченные mock-правила, без NVIDIA для этого шага. Проверен один реальный запрос на синтетике; замечания не считаются доказанными фактами. Новые библиотеки не добавлялись.


## Необязательное техническое задание — 23.09.2026

ReportLab 4.4.9 — генерация PDF; fonts-dejavu-core в Docker — кириллический шрифт. pypdf 6.1.1 — проверка PDF в тестах. pypdfium2 4.30.0 использован только локально для визуальной проверки страниц, не входит в runtime. Генератор ТЗ использует существующие OpenAI/NVIDIA SDK и настройки моделей; контракт проверен тестовым клиентом, live-вызов именно для ТЗ не подтверждён. Mock-шаблон явно обозначен. Новых внешних датасетов нет. Независимое ревью выполнено Claude через оркестратор; реализация и интеграция — Codex.

## GPT-5.5 и второе мнение NVIDIA — 23.09.2026

Модель OpenAI по умолчанию изменена на gpt-5.5, reasoning low. Реальные локальные запросы на синтетике прошли для уточнения/цитат, проверки карточки и ТЗ. Это проверка интеграции, не оценка общей точности; старый EVAL-REVIEW.md относится к прежней модели и не переносится на GPT-5.5. NVIDIA meta/llama-3.3-70b-instruct подключён к резерву всех трёх AI-функций и отдельной проверке; ключ оставлен пустым по решению капитана. Его живое поведение не проверено. Оба провайдера получают исходные поля; отдельный NVIDIA-запрос не получает замечания OpenAI. Валидация схем и цитат выполняется кодом, решение остаётся у человека. Новых библиотек и датасетов не добавлено.
