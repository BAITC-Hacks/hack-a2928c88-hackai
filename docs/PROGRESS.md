# Почасовой прогресс (п. 6.6)

Каждый час соревновательной части — одна проверяемая запись + коммит + тег `hour-N` (`scripts/checkpoint.*`).
Старт соревновательной части: <время из объявления организатора>.

## Час 0 — подготовка (до старта)
- Импортирован заранее подготовленный каркас (раскрыт в docs/DISCLOSURE.md). Предметной логики нет.
