# <Название> — <одна фраза: что делает и для кого>

**Трек:** <№ название> · **HackAlem AI 2026** · **Команда:** Арман — ядро и деплой; Айдана — данные, правила, проверка; Нурсауле — продукт, README, питч

**Демо:** <https://…> · **Доступ:** <логин/пароль или «без авторизации»> · **Видео (резерв):** <ссылка>

## Проблема
<Кто, что делает сейчас, сколько времени/денег/ошибок. 3–4 строки. Источник цифр или «оценка команды».>

## Решение
<Одна фраза решения.> Агент выполняет шаги:
1. **Извлечение** — <что> с цитатой и локатором источника.
2. **Проверки кодом** — <правила: даты, суммы, обязательные поля…>.
3. **Смысловой разбор** — <что ищет модель>.
4. **Подтверждение человеком** — пользователь принимает/отклоняет каждую находку, выгружает отчёт.

<Скриншот или GIF основного сценария>

## Как проверить за 2 минуты
1. Открыть <URL>.
2. Выбрать пример «<имя>» → «Проверить».
3. Ожидаемо: <какие находки появятся>.
4. Нажать «Принять»/«Отклонить», затем «Отчёт».

## Результаты проверки
| Набор | N | Метрика | Наше решение | Бейзлайн (<какой>) |
|---|---:|---|---:|---:|
| <описание, происхождение> | | | | |

Как воспроизвести: `python -m eval.run_eval`. Режим: live (<модель>).

## Технологии
Python 3.12, FastAPI, Pydantic, OpenAI Responses API (Structured Outputs), <модели>. Разработка с Codex (журнал: `docs/CODEX_LOG.md`). Деплой: <платформа>.

Архитектура: `app/agent.py` (конвейер) → `app/rules.py` (детерминированные правила) → `app/llm.py` (модель, журнал вызовов, fallback).

## Запуск локально
```bash
cp .env.example .env   # вписать OPENAI_API_KEY
pip install -r requirements.txt
uvicorn app.main:app --port 8000
# без ключа: MOCK=1 uvicorn app.main:app
python -m pytest -q
```
Docker: `docker build -t app . && docker run -p 8000:8000 --env-file .env app`

## Ограничения (честно)
- Данные: <реальные разрешённые / синтетические — какие именно>.
- Имитируется: <интеграции, которых нет>.
- Не является: <юр. заключением / диагнозом / решением о …>; решение принимает человек.
- Хранение результатов — в памяти процесса (демо).

## Развитие
- Пилот: <кто, какие данные/доступы нужны>.
- Следующие шаги: <2–3 пункта>.
- Масштаб: <другие документы/регионы/ведомства>.

## Раскрытие (п. 6.4)
Заранее подготовленный технический каркас и сторонние материалы перечислены в `docs/DISCLOSURE.md`. Вся предметная логика создана 23.09.2026 во время соревнования (см. `docs/PROGRESS.md`, теги `hour-N`).
