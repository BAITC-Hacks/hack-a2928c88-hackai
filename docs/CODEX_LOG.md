# Вклад Codex

- 23.09.2026: поиск и импорт актуального кейса 07, извлечение критериев и обязательного демо в coord/notes/scoring-07.md; запуск совета и двух задач через scripts/agents.py.
- Схемы Draft/TaskCard/RatingBreakdown/TeamProfile/Proposal/Decision; детерминированный рейтинг с подтверждением точных значений, снятием подтверждения при редактировании, расшифровкой и недостающими полями; тесты границ, контрактов и неподтверждённых значений.
- Исправление обработки маркеров ответа и конфликтующих имён фоновых логов оркестратора; ошибка CLI не должна считаться голосом против.

- 23.09.2026, реализация backend: app/main.py, app/store.py, app/llm.py, общий app/static/index.html/api.js, API/AI-тесты, Dockerfile/render.yaml, инструкции запуска и безопасный checkpoint без перезаписи тегов. Человеческие UI-файлы не редактировались.

- Интеграция и ревью каталога Nursaule aa34489, браузерный smoke в Edge, проверка Docker и HTTP, инструкция Render. Скрипты scripts/smoke_url.py и scripts/smoke_catalog.py; app/static/api.js. UI-файлы участника сохранены без правок.

- Сверка полного PDF и аудит покрытия требований, фиксация первоисточника; подготовка scripts/build-image.ps1, приватного Docker Hub и linux/amd64-образа для Render Existing Image. Учётные данные использованы через credential helper, не записаны в исходники/логи.

- Проверка публичного Render URL через HTTP и Edge/Playwright, фиксация фактических ограничений и ссылки в README/DEPLOY; coord/notes/render-smoke.md.

- Проверка свежих веток участников и 36 тестов; подготовка двух конкретных ТЗ по найденным несоответствиям и состояниям формы, обновление docs/TEAM.md и coord/TASKS.md. Код участников не изменялся.

- Реальная интеграция OpenAI gpt-4.1-mini проверена Codex на синтетическом тексте: отдельный probe и полный Edge-сценарий; добавлены scripts/check_ai.py и scripts/smoke_e2e.py. Код экрана бизнеса участника интегрирован без правок. Исправлены seed evidence и не-JSON ошибки адаптера по независимому ревью Claude.

- Полная браузерная проверка публичного Render с реальным OpenAI; расширен smoke_e2e.py явным флагом --allow-remote и маркировкой синтетических записей. Обновлены фактические результаты и README.

- Расширение синтетики Claude на 400 задач: воспроизводимый генератор, 413 карточек и 417 откликов; additive seed сохраняет существующие записи. 38 тестов и браузерный сценарий каталога пройдены на отдельной локальной базе.
- 23.09.2026: по явному поручению пользователя Codex завершил TASK-01 в business.js/business.css: блокировки ввода, однократные шаги, разделение подтверждений и явное восстановление после конфликта; api.js передаёт HTTP status и русскую сетевую ошибку. 36 pytest passed, DOM с реальным локальным API и браузерный сценарий 0→90→80→90→каталог passed. Независимые ревью Claude Q-150332 и Q-150640 без блокеров. Деплой этих исправлений ещё ожидает доступа к существующему Render и нового приватного образа.

- Обновлён README по main e229006: редизайн, серверный поиск и страницы, рекомендации по словам, 47 пройденных тестов и проверенный локально образ e2290063b6d6. Отдельно отмечены неподтверждённый деплой и незавершённые insights. Подготовлены анализ развития и два ТЗ разработчику 2, создан draft PR #3; Claude не вызывался.

- Hour-3: Codex зафиксировал состояние main, проверок, синтетики и Docker-артефакта; подготовил часовой коммит и тег через scripts/checkpoint.ps1. Нереализованные возможности и неподтверждённый деплой отмечены отдельно.

- Реализован стресс-тест карточки: app/review.py, app/insights.py, main.py, журналирование AI, api.js и экран бизнеса; tests/test_review.py, scripts/smoke_review.py. Проверены гонка revision, неизменность баллов/подтверждений, источник цитат, отсутствие утечки неопубликованного анализа. Каталог разработчика 2 подключён без правок его файлов; README и контракт обновлены.


## 2026-09-23 — необязательное ТЗ и PDF

По запросу заказчика Codex добавил app/specification.py, spec_routes.py, spec_pdf.py, редактор static/specification.js/css; интегрировал переключатель, выбор идей, ручное утверждение, PDF и студенческий снимок. База — main a09b084 с сохранением параллельной реализации проверки карточки; правки интеграции business.js/catalog.js выполнены ведущим по запросу пользователя. 74 pytest, браузерный mock-сценарий и визуальная проверка двухстраничного PDF. Ревью Claude Q-164720 выполнено через scripts/agents.py; замечания по тайм-ауту, логам и UX учтены. Live AI для ТЗ и Render-деплой не проверены.

## 23.09.2026 17:01 — восстановление и проверяемый этап

- Render повторно проверен: редизайн совпадает с main a09b084, API review доступен; публичный браузерный сценарий с OpenAI прошёл. Синтетическая карточка 902b7bfa-b1d6-44b2-9244-b3947dfe9eb9 оставлена для проверки.
- business.js сохраняет только ID последней карточки и предлагает продолжение после F5; подтверждения загружаются с сервера. Несохранённый ввод защищён предупреждением.
- app/stages.py и static/stages.js: фиксированные условия первого этапа, ручная отправка, доработка, приёмка с основанием и однократные +10. История и снимок условий сохраняются в SQLite. Роли остаются демонстрационными.
- AI-review: точные цитаты, запрет вопросов про необязательную реализацию, совместный показ AI и правил. Два прогона одного набора из 12 синтетических карточек: совпадения полей 8→9, live 10→11; это диагностика, не независимая оценка точности.
- 63 pytest passed; smoke_e2e, smoke_review, smoke_stages прошли в локальном браузере. Claude не вызывался по просьбе капитана; независимого агентского ревью нет. Новые функции ещё требуют обновления Render.

## 23.09.2026 — интеграция PR #4 и #5

- PR #4: сравнение сохранённых и опубликованных условий; независимое ревью и браузерный сценарий каталога без блокеров, слит в main.
- PR #5: объединены ТЗ/PDF, восстановление после F5, AI-review и первый проверяемый этап. Сохранена обработка null в API этапа. Несохранённое ТЗ участвует в предупреждении о потере ввода; для устаревшего локального черновика есть явный сброс с подтверждением.
- Сравнение условий включает текст опубликованного ТЗ и выбранных идей; даты, провайдер и внутренние ID не вызывают ложное изменение.
- 78 pytest passed; локальные smoke_e2e, smoke_review, smoke_stages проходят. Независимые Codex-reviewers проверили обе ветки; Claude в ходе интеграции не вызывался. Проверки AI-генератора ТЗ выполнены в mock/SDK-заглушке, новый Render-деплой не выполнен.

- Подготовлен Docker-образ 3f12d9b207d6 после обоих PR: проверены новые PDF-зависимости/шрифт в Linux, API и отсутствие .env; образ загружен в приватный реестр, README/DEPLOY обновлены. Render автоматически не менялся.

- Hour-4: зафиксированы результаты интеграции двух PR, 78 тестов и проверенного Docker-образа; часовой коммит и тег через scripts/checkpoint.ps1.

## 23.09.2026 — GPT-5.5 и NVIDIA

- Добавлены единая конфигурация моделей, GPT-5.5 reasoning low, запас времени/выхода для reasoning. NVIDIA резервирует извлечение, review и ТЗ; JSON/цитаты валидируются, исправление не более одного раза с учётом бюджета.
- Отдельное второе мнение: POST /api/cards/{id}/review/nvidia, собственный снимок nvidia_review, UI, If-Match, неизменность полей/баллов, сохранность ошибок, устаревание и перенос при подтверждении. NVIDIA_API_KEY пуст; принудительная live-проверка не подменяет недоступную NVIDIA другим провайдером.
- 94 теста проходят. Три реальные проверки GPT-5.5 на синтетике успешны. Браузерные проверки review/NVIDIA mock прошли. Независимое ревью Codex без блокеров; Claude не вызывался. docs/TESTER-SCENARIOS.md — 16 ручных сценариев с ожидаемыми результатами и шаблоном дефекта.
