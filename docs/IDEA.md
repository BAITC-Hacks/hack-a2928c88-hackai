# Sana Hub — «Паспорт готовности»

**Статус: капитан утвердил идею; архитектура и план в работе.** Трек 07 «Образование», AI Sana Challenge Hub. Требования: docs/CASE.md. Совет: coord/council/1327-idea/DECISION.md; проверка голосов и ограничений: LEAD_AUDIT.md. 23.09.2026. Две небольшие задачи людям: docs/TEAM.md.

## Одна фраза

Бизнес превращает короткое описание в подтверждённую карточку: видит источник каждого факта, прибавку за полезное дополнение и новое место в открытом каталоге, а затем сам выбирает студенческие команды по их предложениям.

## Почему

1. Сквозной сценарий, геймификация и каталог дают 60/100 баллов кейса. Прозрачное начисление и рабочий выбор важнее дополнительного чат-бота или сложного матчинга.
2. Отличие видно в действиях: ответ → подтверждение → объяснимый рост → изменение позиции. Источник рядом с полем и проверка цитат делают AI-функцию проверяемой.
3. Синтетические данные разрешены; закрытый API не нужен. Схемы и арифметика уже проверены. Один FastAPI-процесс, HTML и SQLite удерживают объём. Оценка 210 минут условная до первого деплоя и сквозного прогона.

## MVP scope: отличия и бюджет 210 минут

| Результат | Отличие | Время |
|---|---|---:|
| Паспорт начислений и минимальный сервер по URL | Семь шкал, подтверждённые элементы, недостающее, «было → стало», уровень | 30 мин |
| AI-интервью | Минимум три разных уместных вопроса; раздел и максимально возможная прибавка; уточнение расплывчатых ответов | 30 мин |
| Источник рядом с фактом | Структурированный ответ, точная цитата, проверка источника, редактирование, ручное подтверждение, live/mock | 30 мин |
| Открытый выбор | Фактическое место, сортировка и оба фильтра; отклик с идеей/планом/сроком/ссылкой; бизнес выбирает одну, несколько или никого; отдельные +10 за подтверждённый этап один раз | 50 мин |
| Проверяемое демо | Данные 5×4, тесты инвариантов, README, проверка хранения и URL, репетиция | 40 мин |
| Резерв | Исправления вместо новых возможностей | 30 мин |

Время включает базовый сценарий, а не только дополнительные фичи. После подтверждения плана UI и backend распределяются между доступными агентами. Gemini в критическом пути нет.

## AI и доказательства

AI находит пробелы и предлагает формулировки, но не добавляет неизвестные факты, не подтверждает поля и не выбирает команду. У фактического утверждения — источник и точная цитата. Отсутствующий источник или несовпадение блокируют перенос утверждения в публикацию. Пользователь может исправить текст сам; ручной ввод становится новым источником.

Совпадение цитаты не доказывает верность пересказа: «CRM нет» нельзя превращать в «CRM доступна». Смысл проверяет человек; отрицания входят в тесты. Не обещаем полную защиту от галлюцинаций или истинность слов бизнеса. Невалидный AI-ответ для проверки защиты показываем как явно искусственный тест, не как ошибку live-модели.

При отказе API — маркированный mock с промптом, форматом входа/выхода и обработкой ошибки. Заглушка не дописывает сведения учебного сценария в произвольную пользовательскую задачу.

## Рейтинг: готовая основа и предложение совета

**Готово:** веса 20/20/15/15/10/10/10; уровни 0–39/40–69/70–89/90–100; точные подтверждения значений, снятие при правке, расшифровка и недостающее. Контекст/потребность по 10, связь 4/3/3. Остальные поля получают полный вес после подтверждения непустого значения. Это подтверждённая заполненность, не смысловая экспертиза.

**После «да» совет предлагает расширенную рубрику с теми же весами:** данные — материал 10 + доступ 10 (доступ требует материала); успех — показатель 5 + условие 5 + проверка 5 последовательно; пользователи — группа 5 + сценарий 5; связь — 4/3/3, консультации и обратная связь требуют контакта. Неизвестные значения дают 0. Подполя подтверждает человек; AI баллы не назначает. Это следующий шаг архитектуры, пока не готовый код. При принятии обновить тесты, данные и демо; не держать две формулы.

До подтверждения рейтинг всегда 0. Подтверждённую карточку можно публиковать при 0–39: низкий балл не скрывает её и не запрещает отклики. Фактическое место считается по опубликованным карточкам; при равенстве — время первой публикации и ID. Предварительные #2/#6 не обещаем.

Совет предлагает редактируемый черновик и последний опубликованный снимок. Отклик сохраняет условия, на которые команда ответила. Полной истории версий нет. +10 за этап — наша учебная величина, не обязательное число ТЗ; только выбранной команде после ручного подтверждения, однократно.

## Данные и демонстрация

data/samples/ai-sana-synthetic.json содержит по 5 черновиков разной полноты, карточек, команд и откликов. Всё маркировано synthetic; контакты и ссылки example.com вымышлены, действующие прототипы не заявляются. Карточки — ручной учебный эталон, а не автоматически подтверждённый вывод AI. T1 содержит сведения из ответов после уточнения, а не только из слабого D1.

Баллы текущей основы пересчитаны кодом: 49, 34, 67, 82, 100. Это тестовый набор, не метрики продукта. docs/DEMO_SCRIPT.md: слабый ввод → вопросы → источник/правка → подтверждение и рост → каталог → новый отклик → ручное решение, затем короткое подтверждение этапа и проверка защиты.

Для базовой формулы учебный рост: 0 → подтверждение потребности 10 → ответы 49 → данные и приёмка 84. При расширении рубрики цифры пересчитать. Все переходы п.11 должны работать в приложении, не на слайдах.

## Не делаем / Later

Auth, пароли, чат, календарь, уведомления, векторную БД, обучение модели, полный трекер, ML-рекомендации, AI-ранжирование команд, автоматическое назначение, отдельный лидерборд, сравнительное исследование с ChatGPT. Не строим универсальный regex-детектор фактов; не начисляем за длину текста. Не заявляем пилот или интеграцию с AI Sana.

## Риски и сокращение объёма

- Если к 90-й минуте нет ввода → вопросов → карточки → рейтинга, убрать дополнительные визуализации, сравнение откликов бок о бок и рекомендации. Оба фильтра и все обязательные переходы сохраняются.
- Проверить SQLite на выбранном размещении после перезапуска; seed только в пустую БД. Временное хранение и сброс раскрыть в UI/README, если постоянного нет.
- Структурная проверка и человеческое подтверждение не доказывают правдивость бизнеса; рейтинг не оценивает реальную ценность проекта.
- API/UI/LLM/URL сейчас отсутствуют. Первый деплой в первом часу — следующий обязательный этап после подтверждения, не достигнутый результат.
- Riipen уже имеет AI-создание проектов; не заявлять уникальность самого конструктора. Разведка и 5 приёмов: coord/notes/research-07.md. Пилот с координатором и несколькими бизнесами — гипотеза, договорённостей нет.

## Голоса и разногласия

- **Codex: AGREE**, итоговый vote1: закрепить однократный прогресс UNIQUE(задача, команда, этап) и транзакцией, проверить повторные и конкурентные запросы.
- **Claude: AGREE**, итоговый vote1: заранее определить сокращения. Предложение убрать фильтр темы отклонено ведущим: он обязателен по п.3 ТЗ.
- **Gemini: UNAVAILABLE** — Antigravity без авторизации, Gemini CLI IneligibleTierError. Старый процесс ошибочно вывел DISAGREE для ошибки, содержательного несогласия и третьего голоса нет.
- Codex предлагал извлечение цитат, Claude — читаемую переформулировку. Совет выбрал читаемую карточку с точными источниками и человеческой проверкой, без обещания полной автоматической проверки смысла.
- Предварительную позицию заменили фактической после публикации. Совет сохраняет опубликованный снимок; упрощение версий при нехватке времени не должно закрывать доступ к опубликованным задачам.
- Один промежуточный ответ Claude был усечён парсером маркеров; полный лог восстановлен, финальный ответ и голос прочитаны ведущим. Ограничение качества совета отражено в LEAD_AUDIT.md.

## Готовность и следующий шаг

Схемы и базовый рейтинг: **26 тестов проходят**. Read-only ревью Claude — OK, тесты у себя он не запускал. API, UI, продуктовый LLM, публикация и деплой не реализованы. После «да»: council arch → council plan → задачи на доске → параллельная реализация → первый деплой и checkpoint. До подтверждения эту стадию не начинаем.


---

<!-- council 1355-plan -->
# Решение совета: План работ по часам и разбивка на задачи агентам

Время: 23.09 13:59. Участники: codex, claude. Раундов критики: 1. Консенсус: нет — см. голоса.

## Решение

Единый план MVP: **черновик → минимум три уточняющих вопроса → редактирование и подтверждение карточки → объяснимый рейтинг → публикация → новый отклик → ручной выбор или отклонение → подтверждённый прогресс**.

Это итоговое предложение совета, не заявление о достигнутом консенсусе: Claude не ответил, Gemini недоступен.

**Для MVP оставляем базовую формулу:** контекст и потребность — 10+10; данные — 20; результат — 15; критерии успеха — 15; ограничения — 10; пользователи — 10; связь — 4+3+3. Начисляем только за непустые подтверждённые значения. Расширенные подполя откладываем. Это явная поправка к архитектурному предложению; перед реализацией ведущий фиксирует её согласование и обновляет документы. Две формулы не поддерживаем.

**Ответственность:** Codex — backend, AI, SQLite, рейтинг, общий `index.html`, API-адаптер, интеграция, данные и размещение. T-001 сохраняет `business.js/business.css`, T-002 — `catalog.js/catalog.css`; AI-агентам эти файлы не передаём. Claude — независимое ревью при доступности. Gemini задач не получает.

Расписание относительно начала реализации; **210 минут — условный бюджет**. В первые 15 минут сверяем фактический дедлайн и ближайший обязательный checkpoint. Последние 30 минут сохраняем для исправлений.

| Интервал | Результат Codex | Критерий готовности |
|---|---|---|
| 0–15 мин | Аудит файлов, согласование формулы, сопоставление методов TEAM с HTTP, начало размещения | Известны оставшиеся работы, дедлайн, путь SQLite и постоянное хранилище; интерфейсы людей сохранены |
| 15–45 мин | Минимальный сервер, хранение, общий экран и адаптер, создание и правка карточки | URL отвечает; новая запись переживает перезапуск; четыре набора по пять записей валидируются, рейтинг пересчитывается |
| 45–90 мин | AI-вопросы, сборка карточки, источники, подтверждение, рейтинг; интеграция T-001 | Через UI проходит слабый ввод → вопросы → ответы → правка → подтверждение → объяснимый рост |
| 90–120 мин | Публикация, каталог, отклики, решения бизнеса, фиксированный этап; интеграция T-002 | По URL проходит весь сценарий с новым откликом и отдельными +10 |
| 120–150 мин | Проверки ошибок и состояния, независимое ревью готовых срезов | Проверены оба фильтра, низкий рейтинг, отклонение всех, выбор нескольких, версии, снимки и однократное начисление |
| 150–180 мин | Исправления, README, disclosure, внешний smoke и репетиция | Демо до пяти минут; ограничения раскрыты; ревью изменений основного сценария завершено до слияния |
| 180–210 мин | Резерв | Только устранение блокеров и повторная проверка затронутых переходов |

Люди показывают первый экран через 20 минут, готовый модуль и хеш — через 45–60 минут. Интегрируем по готовности. Ревью запрашиваем по готовым срезам, не откладывая всё на последний час.

Обязательные инварианты:

- Правка снимает подтверждение изменённого значения. Неизвестные сведения дают ноль. Низкий рейтинг не запрещает публикацию подтверждённой карточки или отклик.
- Адаптер передаёт версию именно показанной карточки. Конфликт требует обновления и нового подтверждения человеком; автоматического повторного подтверждения нет.
- Каталог использует опубликованный снимок; сортировка — рейтинг по убыванию, затем время первой публикации и ID. Оба фильтра обязательны.
- Отклик сохраняет полный снимок просмотренных опубликованных условий. Повторная публикация прежние условия отклика не меняет.
- Бизнес выбирает ноль, одну или несколько команд. После принятия фиксированного этапа выбранной команде начисляются учебные +10 отдельно от рейтинга задачи. Уникальность `(task_id, team_id, stage_id)` и транзакция предотвращают повторное начисление.
- AI предлагает сведения с источником, цитатой и локатором; человек проверяет смысл. Несуществующая цитата блокирует принятие поля. OpenAI — основной, NVIDIA — резерв, затем явно обозначенный mock; фактический режим виден.

После каждого рабочего среза — `python -m pytest -q`. По фактическим соревновательным часам — commit, тег `hour-N`, запись прогресса, включая частичный результат. Работа и коммиты остаются в выданном репозитории; новые зависимости и вклад Codex документируются.

## Почему так

1. Сквозной сценарий, геймификация и каталог дают 60/100 баллов CASE; полный путь должен появиться раньше заключительной проверки.
2. Базовая формула уже предусмотрена текущими схемами и контрактом людей. Расширение потребовало бы новых подтверждаемых подполей, тестов и переработки данных.
3. Ранний URL и проверка перезапуска отдельно выявляют проблемы размещения и хранения.
4. Версии, снимки и транзакции защищают конкретные действия пользователя: подтверждение, отклик и начисление.
5. Отсутствие Claude не останавливает реализацию, но и не подменяет обязательное независимое ревью.

## Что отвергли и почему

- Расширенную рубрику в текущем MVP — увеличивает объём и расходится с контрактом TEAM.
- Перенос прогресса и ручного отклонения на конец — оставляет обязательный сценарий незавершённым слишком долго.
- Изменение четырёх человеческих UI-файлов агентами — нарушает распределение ответственности.
- Порог допуска по рейтингу, автоматическое назначение команды, удаление одного из фильтров — противоречат CASE.
- Новые возможности сверх MVP: полный трекер, авторизацию, чат, ML-ранжирование и дополнительные визуализации — не включаем в критический путь.
- Ожидание гарантированного возвращения Claude в 14:50 — сброс лимита не доказывает доступность.

## Риски и как снимаем

- **Неизвестный остаток времени:** привязываем интервалы к фактическому дедлайну. Если к 90-й минуте нет конструктора с рейтингом, прекращаем оформление; если к 120-й нет полного пути, следующий интервал целиком отдаём обязательным переходам.
- **Потеря данных:** постоянное хранилище, seed только в пустую БД, проверка новой записи после перезапуска. Временное хранение раскрываем как ограничение, не называем постоянным.
- **Ошибки AI:** проверяем неверную цитату, отрицание, невалидный ответ и отказ API. Совпадение цитаты не объявляем доказательством смысла; показываем промпт и формат обмена.
- **Ошибки состояния:** проверяем границы уровней, снятие подтверждения, устаревшую версию, сохранность условий и повторные/конкурентные начисления.
- **Недоступное ревью:** запрашиваем Claude по готовым срезам. При отсутствии ответа слияние изменений основного сценария остаётся заблокированным правилами репозитория; ведущий выносит вопрос капитану, не записывает фиктивное одобрение.
- **Недостоверная демонстрация:** synthetic/mock обозначаем в UI и README; проверяем новый отклик и ручное решение по работающему URL. Доступность на период 24–28.09 проверяем отдельно от локального запуска.

## Разногласия

Codex ранее поддержал расширенную рубрику в архитектурном раунде, теперь предлагает базовую ради совместимости и сроков. Это изменение позиции требует явного согласования и фиксации ведущим; текущий ответ сам по себе не меняет архитектурный документ.

Claude не представил содержательной позиции по плану из-за лимита сессии. Его согласие или несогласие неизвестно. Gemini недоступен и голосом не учитывается. Единогласие не заявляем.

## Задачи

TASK | codex | Зафиксировать сроки, формулу и контракт: определены дедлайн/checkpoint, согласована поправка к архитектуре, методы TEAM сопоставлены с HTTP без изменения интерфейсов людей.
TASK | codex | Развернуть минимальный сервер и SQLite: URL отвечает, новая запись переживает перезапуск, seed не дублируется, четыре набора по пять записей валидируются.
TASK | codex | Реализовать конструктор и интегрировать T-001: минимум три уместных вопроса, источники, правка, снятие подтверждения и объяснимый пересчёт работают через UI.
TASK | codex | Реализовать публикацию и интегрировать T-002: оба фильтра, сортировка, отклик при низком рейтинге, отклонение всех и выбор нескольких работают по URL.
TASK | codex | Реализовать версии и прогресс: отклик сохраняет просмотренные условия, устаревшее подтверждение отклоняется, выбранная команда получает +10 ровно один раз при повторных и конкурентных запросах.
TASK | claude | При доступности провести независимое ревью готовых срезов и финального сценария: выдать замечания с проверками; до слияния устранены блокеры, ошибка запуска не считается ревью.
TASK | codex | Подготовить сдачу: проходят pytest и внешний smoke, README содержит запуск, формулу, URL и ограничения, disclosure обновлён, демо укладывается в пять минут, почасовые checkpoints подтверждены.

## Голоса

### codex: AGREE
AGREE
Главная поправка: недоступность независимого ревью вынести капитану в первые 15 минут, чтобы определить допустимый порядок слияния до накопления готовых изменений.

### claude: UNAVAILABLE
[claude не ответил, rc=1]
You've hit your session limit · resets 2:50pm (Asia/Qyzylorda)

## Материалы

Предложения и критика: `coord/council/1355-plan/`


## Решение ведущего по объёму

Базовая формула app/rating.py остаётся единственной для MVP. Расширенные подполя — Later. Проверенные цитаты не считаются доказательством истины или смысловой корректности. Основной сценарий реализован через API; UI и публичное размещение не объявляются готовыми до проверки.


## Дополнение по запросу заказчика: ТЗ

23.09.2026 заказчик явно расширил сценарий: необязательное AI-ТЗ, ручная правка и выбор идей, утверждение и PDF для студентов. Это разрешённое дополнение к MVP. Рейтинг не меняется; студентам передаётся опубликованный утверждённый снимок. Контракт и ограничения: [TECHNICAL-SPEC.md](TECHNICAL-SPEC.md).
