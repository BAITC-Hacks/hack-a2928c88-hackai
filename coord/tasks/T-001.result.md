# T-001 — business UI reliability

User explicitly authorized Codex to complete TASK-01 and integrate/deploy it, superseding the earlier human-only ownership of business.js/business.css for this task.

## Changes

- Lock inputs, checkboxes and buttons during requests; restore each original disabled/readOnly state. Newly rendered editor controls stay locked between save and confirm.
- Freeze the original source as soon as a draft exists, including clarification failure. Retry clarification against that draft; New task resets it. Prevent replaying clarification/build after advancing.
- Separate pending confirmation checkboxes from confirmed_fields returned by the API. Editing clears pending consent, saving does not reconfirm. Confirm can explicitly save and then confirm in separate requests.
- On version conflict, retain edits and require an explicit getCard reload with a visible replacement warning. Reload clears pending consent; no automatic confirmation retry.
- Integration: shared API errors expose HTTP status and a Russian network error without changing API methods or backend behavior.

## Validation — 2026-09-23

- Python 3.12, existing pinned requirements: 36 pytest tests passed.
- DOM acceptance with jsdom 26.1.0, the real api.js and a running local FastAPI in MOCK=1: passed. No replacement API or new product mock, AI or rating formula.
- Covered six clarification questions, zero initial score, confirmation/growth, edit/drop, reconfirmation, save+confirm in one action, disabled empty field, network failure, partial create/clarify retry, duplicate submit, obsolete steps, new DOM locking, real 409 from a second API writer, explicit reload clearing consent, publication/callback/catalog, and reset.
- Browser acceptance through Codex browser: weak description → six questions → card → 0 → 90 → edit/save → 80 → reconfirm → 90 → publish → automatic catalog switch and new card visible.
- Independent Claude read-only review and API follow-up: no blockers; reports in coord/notes/Q-150332-claude.md and Q-150640-claude.md. Reviewer assessed code; test execution was performed by Codex.

## Integration / deploy

Based on remote main 04d4c6c, which already includes original business commit 9cafaa7 and catalog. Local git clone was unavailable due to missing Git credentials; source was read via the authenticated GitHub connector. Remote changes use the original GitHub tree and ancestry, not the local QA snapshot history.

Deployment is not yet complete. Existing Render uses a private Docker Hub image. This machine has no Docker installation, and access to the existing Render service is still being completed by the user. Do not describe local acceptance or a GitHub merge as a successful Render deployment.
