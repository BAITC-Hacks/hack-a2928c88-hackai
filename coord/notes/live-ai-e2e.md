# Live OpenAI и полный UI-сценарий — 23.09.2026

Ключ предоставлен капитаном в игнорируемом .env. В файл добавлены только настройки MOCK=0, OPENAI_MODEL=gpt-4.1-mini и FALLBACK_TO_MOCK=1; значение ключа не выводилось. Приложение в новый образ ключ не включает.

- scripts/check_ai.py: один реальный запрос на синтетическом черновике, provider=openai, mode=live, 3 вопроса, R-QUOTE passed, ошибок нет.
- scripts/smoke_e2e.py --mode live: локальный сервер 8002 с отдельной БД. Два реальных вызова — уточнения и карточка. Edge прошёл ввод → ответы → ручные правки → подтверждение → 0/100/80/100 → публикацию → отклик → ручной выбор → +10 → повтор без начисления. Никаких новых фактов без ручного ввода/подтверждения не добавлено.
- После исправлений ревью: полный mock E2E на отдельной БД/порту 8003, включая ответ сервера 503 text/plain; понятное сообщение вместо SyntaxError. 36 pytest passed.
- Картинки проверки в logs/e2e-live.png и logs/e2e-mock.png, не коммитятся.

Ограничения: это локальная проверка. На опубликованном Render всё ещё старый образ и mock; требуется обновить image и Environment. Browser smoke не заменяет репетицию доклада и не подтверждает устойчивость всех взаимодействий при ошибках — эти задачи у владельцев T-001/T-002.

## Разбор ревью Claude T-008

- B1: подтверждённое несоответствие подписи ссылки и контракта. URL остаётся обязательным; владелец T-002 исправляет форму. Сценарий с заполненной ссылкой работает.
- B2: локальный live включён и проверен, Render требует настройки капитаном.
- B3: гипотеза о несовместимости схемы не подтвердилась — успешные реальные вызовы текущей схемы.
- E1: новые синтетические карточки не получают псевдоцитат собственного текста (evidence={}). Старые записи уже существующих БД сохраняются; seed не перезаписывает пользовательские данные.
- S3: адаптер корректно обрабатывает plain-text HTTP 503, проверено браузером.
- M1/M2: progress_points — уже заработанный баланс команды по задаче, не награда за новый pending-отклик. Изменение решения не отменяет ранее подтверждённый этап; автоматического повторного начисления нет. Это текущая модель MVP.
- Demo-роли и открытое чтение записей раскрыты как ограничение; производственная авторизация не заявляется.
