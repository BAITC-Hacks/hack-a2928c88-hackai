## Q-133528: ревью `schemas.py`, `rating.py` и `test_rating.py` по CASE.md п.4

**Вердикт: OK.** Реальных ошибок не нашёл. Тесты не запускал: команда запуска pytest не получила разрешения. Всё ниже основано только на чтении кода.

- **Веса:** 20/20/15/15/10/10/10 = 100, совпадают с таблицей п.4 (`app/rating.py:9-17`). Деление внутри групп (10+10 и 4+3+3) — выбор команды, в коде так и помечено. Эту формулу нужно описать в README (п.10). Поле `title` баллов не даёт, это правильно.
- **Границы уровней:** 39/40, 69/70, 89/90 верны. Значения `True` и `40.5` отклоняются (`rating.py:21`). `recommended_eligible` включается при `total >= 40`, как и сказано в п.4.
- **Подтверждение:** баллы даются только за непустое поле, у которого подтверждённое значение совпадает с текущим (`rating.py:32-35`). Что ИИ сам ничего не подтвердит, обеспечено: `AISuggestion` запрещает лишние поля.
- **Снятие при правке:** `edit_card` удаляет подтверждение изменённого поля. Прямое присваивание тоже не даёт баллов: сравнение идёт по значению. Исходная карточка при этом не меняется.
- **Мелочь 1:** после прямого присваивания устаревшая запись остаётся в `confirmations`. В будущем UI статус «подтверждено» надо брать из `is_confirmed`, а не из ключей этого словаря.
- **Мелочь 2:** тест `test_title_is_required_in_card...` называется неточно. Обязательность `title` нигде не проверяется (по умолчанию `""`), тест проверяет только то, что `title` не приносит баллов.
- **Мелочь 3:** у полей `CardContent` нет `max_length`, а у `Draft` лимит 20000 символов. Это не ошибка основы, но на уровне API лимит понадобится.

Файлы не менял.
